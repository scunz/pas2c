#include "TokenTypes.hpp"

/*
LexicalAnalyzer::Ptr LexicalAnalyzer::create(const Errors::Ptr& errors, const QString& fileName)
{
    QScopedPointer<LexicalAnalyzerImpl> anal = new LexicalAnalyzerImpl(errors, fileName);

    if (anal->analyze()) {
        return anal.take();
    }

    return NULL;
}
*/
