
PROJECT(PAS2C_CORE_UTILS)

QT_PREPARE(Core -Gui)

INCLUDE(files.cmake)
SET(SRC_FILES ${_SRC_FILES})
SET(HDR_FILES ${_HDR_FILES})

ADD_QT_LIBRARY(
    pascCoreUtils ${LIB_SCOPE}

    ${SRC_FILES}
    ${HDR_FILES}
)
